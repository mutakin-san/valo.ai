<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.capstone.valoai">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Valoai">
        <activity
            android:name=".features.profile.presentations.EditVaksinActivity"
            android:exported="false" />
        <activity
            android:name=".features.profile.presentations.EditProfileActivity"
            android:exported="false" />
        <activity
            android:name=".features.profile.presentations.ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".features.dashboard.presentations.DashboardActivity"
            android:exported="false" />
        <activity
            android:name=".features.auth.presentation.register.FormPersonalActivity"
            android:exported="false" />
        <activity
            android:name=".features.auth.presentation.register.RegisterActivity"
            android:exported="false" />
        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".features.maps.presentation.VaccineLocationMapsActivity"
            android:exported="false"
            android:label="@string/title_activity_vaksin_location_maps" />
        <activity
            android:name=".features.auth.presentation.login.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".features.detail_faskes.presentation.DetailFaskesActivity"
            android:exported="false" />
        <activity
            android:name=".features.onboarding.presentation.OnBoardingActivity"
            android:exported="false" />
        <activity
            android:name=".features.splashscreen.presentation.SplashScreenActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>